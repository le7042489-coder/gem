# GEM Clinical Frontend

## Setup

```bash
cd frontend
npm install
npm run dev
```

Set backend URL if needed:

```bash
export NEXT_PUBLIC_API_BASE=http://localhost:8000
```

Then open http://localhost:3000
